#include "tareabase.h"

TareaBase::TareaBase(const std::string &titulo, const std::string &descripcion, const std::string &fecha)
    : titulo(titulo), descripcion(descripcion), fecha(fecha)
{
}
